spring:
  r2dbc:
    url: r2dbc:postgresql://arka-db-provider:5432/db_provider
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}
  kafka:
    bootstrap-servers: kafka:29092

logging:
  level:
    com.arka: INFO
