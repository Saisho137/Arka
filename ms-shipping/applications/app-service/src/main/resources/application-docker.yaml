# Perfil DOCKER — cuando corre dentro de Docker Compose
spring:
  r2dbc:
    url: r2dbc:postgresql://arka-db-shipping:5432/db_shipping
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}
